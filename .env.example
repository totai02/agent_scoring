AVAYA_BASE_URL=http://localhost:8000
KAFKA_BOOTSTRAP=localhost:9092
DATABASE_URL=postgresql+psycopg2://agent:agentpw@localhost:5432/agent_scoring
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
S3_BUCKET=ccas-audio-dev

# STT Consumer Optimization Settings
STT_MAX_WORKERS=4
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
STT_BATCH_SIZE=10
POLL_TIMEOUT=1.0

# Whisper Model Settings
WHISPER_MODEL_SIZE=base
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8

# Database Settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=agent_scoring
DB_USER=agent
DB_PASSWORD=agentpw

# Topics
TOPIC_CALLS_RAW=calls.raw
TOPIC_CALLS_ENRICHED=calls.enriched
TOPIC_AUDIO_READY=audio.ready

# Processing Settings
MAX_DOWNLOAD_CONCURRENCY=16
POLL_INTERVAL_SECONDS=30
POLL_WINDOW_SECONDS=60
POLL_DELAY_SECONDS=90
